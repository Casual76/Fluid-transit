# Gradle / Android
.gradle/
build/
local.properties
keystore.properties
*.jks
*.apk
*.aab
captures/
.cxx/

# IDE
.idea/
*.iml
.kotlin/

# Node (worker)
node_modules/
.wrangler/
.dev.vars

# Dati di lavoro degli spike: scaricabili, mai versionati.
# Il feed GTFS della Regione e' 129 MB zip / 584 MB estratto.
spikes/**/work/
tools/**/work/
*.ftb
*.pmtiles

# La configurazione Firebase: il repo e' pubblico, e anche se le chiavi di
# google-services.json sono chiavi client (finiscono nell'APK) non c'e' motivo
# di regalarne le quote. Stesso patto di keystore.properties: chi compila
# senza il file ottiene un build identico, solo senza Crashlytics.
app/google-services.json
